{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run start-parallel.ts",
    "worker": "bun run index.ts",
    "db-consumer": "bun run db-consumer.ts",
    "dev": "bun run --watch start-parallel.ts",
    "test-setup": "bun run test-websites.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "redis-stream": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "db": "workspace:*"
  }
}
